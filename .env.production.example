# Production Environment Configuration Example
# Copy to .env.production and adjust values

# Production typically uses standard ports behind reverse proxy
FRONTEND_PORT=3000
API_PORT=8000
CHROMA_GATEWAY_PORT=8000

# Or if running directly exposed:
# FRONTEND_PORT=443
# API_PORT=443
# CHROMA_GATEWAY_PORT=8002

# Container ports remain the same
API_CONTAINER_PORT=8000
CHROMA_CONTAINER_PORT=8000

# API Configuration (use secure keys in production!)
INTERNAL_API_KEY=<generate-secure-key>
NEXT_PUBLIC_BACKEND_URL=https://api.capture.yourdomain.com
NEXT_PUBLIC_BACKEND_API_KEY=<generate-secure-key>

# Additional production settings
NODE_ENV=production
LOG_LEVEL=warning