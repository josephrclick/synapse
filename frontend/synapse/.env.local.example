# Frontend Environment Configuration
# Copy this file to .env.local and update with your values

# Backend API Configuration
# URL for the backend API (used by Next.js API routes)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8101

# API key for backend authentication
NEXT_PUBLIC_BACKEND_API_KEY=your-secret-api-key-here

# Deepgram API Configuration (Optional - for voice transcription features)
# Get your free API key at https://deepgram.com
# Deepgram offers industry-leading speech-to-text accuracy with low latency
# Perfect for real-time transcription, voice commands, and audio processing
NEXT_PUBLIC_DEEPGRAM_API_KEY=your-deepgram-api-key-here

# Note: NEXT_PUBLIC_ prefix makes these variables available in the browser
# Only use for non-sensitive configuration that's safe to expose client-side